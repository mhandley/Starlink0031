using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class CameraVLEO: MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		/*
		if (Input.GetKeyDown ("b") || Input.GetKeyDown (KeyCode.PageUp)) {
			SceneManager.LoadScene ("SP_basic3_revised");
		}
		*/
		/*
		if (Input.GetKeyDown ("n") || Input.GetKeyDown (KeyCode.PageDown)) {
			SceneManager.LoadScene ("00TitleScene");
		}
		*/
	}
}
